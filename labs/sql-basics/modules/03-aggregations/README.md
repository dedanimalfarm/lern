# Модуль 3: Агрегация и группировка данных

## Теория
Агрегатные функции выполняют вычисление на наборе значений и возвращают одиночное значение:
- `COUNT()` — количество строк.
- `SUM()` — сумма значений.
- `AVG()` — среднее значение.
- `MIN() / MAX()` — минимальное / максимальное значение.

Конструкция `GROUP BY` позволяет группировать строки с одинаковыми значениями в указанных столбцах и применять агрегатную функцию к каждой группе.
Конструкция `HAVING` используется для фильтрации результатов **после** группировки (в отличие от `WHERE`, который фильтрует до группировки).

## Задания
Перейдите в директорию `tasks/` для выполнения заданий:
- [Задание 1: Статистика магазина](tasks/01-aggregations.md)
