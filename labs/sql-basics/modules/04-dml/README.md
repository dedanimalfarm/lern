# Лабораторная работа 04: Управление данными (DML)

## Оглавление
- [Часть 1: Транзакции (ACID)](#часть-1-транзакции-acid)
- [Часть 2: Вставка записей (INSERT) и UPSERT](#часть-2-вставка-записей-insert-и-upsert)
- [Часть 3: Модификация (UPDATE) и Удаление (DELETE)](#часть-3-модификация-update-и-удаление-delete)
- [Часть 4: Опасность массовых удалений (Batch Deletes)](#часть-4-опасность-массовых-удалений-batch-deletes)
- [Часть 5: Быстрая очистка таблиц (TRUNCATE) и сравнение с DELETE](#часть-5-быстрая-очистка-таблиц-truncate-и-сравнение-си-delete)
- [Часть 6: Каскадное удаление данных](#часть-6-каскадное-удаление-данных)
- [Часть 7: Troubleshooting — катастрофа забытого WHERE](#часть-7-troubleshooting--катастрофа-забытого-where)
- [Вопросы для самопроверки](#вопросы-для-самопроверки)

Время: ~25 мин | Сложность: 3/5

Цель: изучить конструкции языка Data Manipulation Language для добавления, изменения и удаления информации, понять важность транзакций, а также узнать правила безопасной работы с большими объемами данных в production-среде.

---

## Часть 1: Транзакции (ACID)

Любая модификация данных в реляционной СУБД должна подчиняться правилам ACID (Atomicity, Consistency, Isolation, Durability). Команды `BEGIN`, `COMMIT` и `ROLLBACK` управляют границами транзакции. Если в процессе модификации нескольких таблиц произошла ошибка, транзакция должна быть откачена (`ROLLBACK`), чтобы не оставить базу в несогласованном состоянии.

---

## Часть 2: Вставка записей (INSERT) и UPSERT

Команда `INSERT` добавляет новые строки. Вы можете вставлять несколько строк одним запросом (Bulk Insert), что работает в десятки раз быстрее, чем множество одиночных команд.

```sql
INSERT INTO users (name, email) 
VALUES 
    ('Алексей', 'alex@example.com'),
    ('Елена', 'elena@example.com');
```

Для идемпотентности скриптов автоматизации (например, CI/CD-пайплайнов) крайне полезен механизм **UPSERT** (вставка или обновление при конфликте). Он предотвращает падение скрипта при повторной вставке уникальных значений:

```sql
INSERT INTO products (name, category, price, stock_quantity)
VALUES ('Флешка', 'Аксессуары', 1000, 10)
ON CONFLICT (name) DO UPDATE 
SET stock_quantity = products.stock_quantity + EXCLUDED.stock_quantity;
```

---

## Часть 3: Модификация и Удаление

`UPDATE` модифицирует существующие строки. `DELETE` удаляет их.
**Обе команды требуют предельной осторожности и обязательного указания фильтрации `WHERE`.**

```sql
UPDATE products 
SET price = price * 0.9, stock_quantity = stock_quantity - 1 
WHERE id = 1;

DELETE FROM orders WHERE status = 'CANCELLED';
```

---

## Часть 4: Опасность массовых удалений (Batch Deletes)

> [!WARNING]
> **Критическая ошибка DevOps:** Запуск команды вроде `DELETE FROM orders WHERE order_date < '2025-01-01';` на таблице из 100 000 000 строк может «уронить» базу данных.

**Почему это происходит?**
1. **Блокировки (Row Locks):** База данных заблокирует все удаляемые строки. Приложения не смогут обновить или записать данные в эту таблицу (возникнут таймауты).
2. **Раздувание WAL (Write-Ahead Log):** Каждая удаленная строка пишется в журнал транзакций. Массовое удаление может мгновенно забить свободное место на диске файлами WAL.
3. **Раздувание таблицы (Bloat):** `DELETE` физически не удаляет строки с диска, он лишь помечает их как «мертвые» (dead tuples). Реально место на диске освободится только после тяжелого процесса `VACUUM FULL`, который полностью блокирует таблицу.

**DevOps Best Practice:**
Удаляйте большие объемы данных частями (батчами) в цикле, делая небольшую паузу (`sleep`) между итерациями, чтобы СУБД успевала обрабатывать запросы пользователей:
```sql
-- Пример концепта батч-удаления:
DELETE FROM orders 
WHERE id IN (SELECT id FROM orders WHERE order_date < '2025-01-01' LIMIT 5000);
```

---

## Часть 5: Быстрая очистка таблиц (TRUNCATE) и сравнение с DELETE

Если вам нужно полностью очистить таблицу (например, сбросить кэш или очистить временную лог-таблицу), использование `DELETE FROM table;` неэффективно. Вместо этого применяется `TRUNCATE TABLE`:

```sql
TRUNCATE TABLE orders CASCADE;
```

### Сравнительная таблица: DELETE vs TRUNCATE

| Характеристика | DELETE | TRUNCATE |
| :--- | :--- | :--- |
| **Принцип работы** | Сканирует и удаляет каждую строку по отдельности | Сбрасывает таблицу на диске, удаляя файл данных и создавая пустой |
| **Скорость** | Медленно (зависит от количества строк) | Мгновенно (не зависит от размера таблицы) |
| **Логирование (WAL)** | Полное (пишет каждую удаленную строку) | Минимальное (пишет только факт сброса таблицы) |
| **Блокировки** | Блокирует только удаляемые строки | Полностью блокирует таблицу (`Access Exclusive Lock`) |
| **Освобождение диска** | Место остается занятым (нужен VACUUM) | Дисковое пространство освобождается операционной системе мгновенно |
| **Фильтрация (WHERE)**| Да (можно удалить часть строк) | Нет (очищает таблицу целиком) |

---

## Часть 6: Каскадное удаление данных

Если вы попытаетесь удалить пользователя, у которого есть заказы в таблице `orders`, СУБД заблокирует операцию и выдаст ошибку `Foreign key constraint violation`. Это защита целостности данных, предотвращающая появление "осиротевших" записей.
Чтобы дочерние записи удалялись автоматически вместе с родительскими, при создании внешнего ключа в структуре таблицы указывается правило `ON DELETE CASCADE`.

---

## Часть 7: Troubleshooting

**Сценарий:** На сервере production вы выполняете ручную корректировку цен. Вы пишете:
```sql
UPDATE products SET price = 5000;
```
Нажимаете Enter и понимаете, что забыли `WHERE id = 123`. Все товары в магазине стали стоить 5000. Бизнес остановлен.

**Решение и лучшие практики:** Никогда не выполняйте `UPDATE` или `DELETE` в консоли production напрямую. Всегда оборачивайте их в транзакцию.
```sql
BEGIN;
UPDATE products SET price = 5000 WHERE id = 123;
-- Проверьте, сколько строк было обновлено (UPDATE 1). Если UPDATE 1000 - срочно делайте ROLLBACK.
COMMIT;
```
Если данные уже испорчены и транзакция зафиксирована, единственное спасение — восстановление из резервной копии.

---

## Вопросы для самопроверки
1. Чем команда `DELETE FROM table;` отличается от команды `TRUNCATE TABLE table;` с точки зрения производительности и внутренних механизмов СУБД?
2. Что произойдет при попытке выполнить `INSERT`, если вставляемое значение нарушает ограничение `UNIQUE`?
3. Гарантирует ли транзакция, что изменения будут сохранены на жестком диске даже в случае внезапного отключения питания сервера (D - Durability в ACID)? Как это достигается технически?
4. Почему массовое удаление логов одной командой `DELETE` опасно для продакшн-базы данных? Какие есть альтернативы?
