# Лабораторная работа 05: Индексы и оптимизация запросов (EXPLAIN ANALYZE)

## Оглавление
- [Часть 1: Планировщик запросов и EXPLAIN](#часть-1-планировщик-запросов-и-explain)
- [Часть 2: EXPLAIN ANALYZE — реальное выполнение](#часть-2-explain-analyze--реальное-выполнение)
- [Часть 3: Индексы (B-Tree)](#часть-3-индексы-b-tree)
- [Часть 4: Troubleshooting — почему индекс не используется?](#часть-4-troubleshooting)
- [Вопросы для самопроверки](#вопросы-для-самопроверки)

Время: ~30 мин | Сложность: 4/5

Цель: научиться анализировать производительность SQL-запросов, читать планы выполнения, оптимизировать медленные выборки с помощью индексов и понимать логику работы планировщика СУБД.

---

## Часть 1: Планировщик запросов и EXPLAIN

Прежде чем выполнить любой SQL-запрос, СУБД (например, PostgreSQL) передает его **планировщику (optimizer)**. Планировщик строит дерево возможных путей выполнения запроса, оценивает их «стоимость» (cost) на основе системной статистики таблиц и выбирает самый дешевый план.

Команда `EXPLAIN` показывает план выполнения запроса, выбранный планировщиком, **без фактического запуска самого запроса**. Это безопасно запускать на продакшене для анализа тяжелых запросов.

```sql
EXPLAIN SELECT * FROM users WHERE email = 'user_5@example.com';
```

Вывод команды `EXPLAIN` содержит:
* `Seq Scan` (Sequential Scan) — последовательное чтение всей таблицы с диска.
* `cost=0.00..2.50` — оценка стоимости (cost). Первая цифра (0.00) — стоимость получения первой строки, вторая (2.50) — стоимость получения всех строк. Это безразмерные единицы стоимости (по умолчанию 1 unit = чтение одной страницы с диска).
* `rows=1` — планируемое количество строк в результате.
* `width=34` — средняя ширина строки в байтах.

---

## Часть 2: EXPLAIN ANALYZE — реальное выполнение

Для точной диагностики одной команды `EXPLAIN` часто недостаточно, так как планировщик может ошибаться в оценках (например, если статистика БД устарела). 

Команда `EXPLAIN ANALYZE` **выполняет** запрос в базе данных, замеряет точное время работы каждого шага и выводит как планируемые показатели, так и реальные (`actual time`, `loops`, `rows`).

> [!CAUTION]
> Так как `EXPLAIN ANALYZE` реально выполняет запрос, будьте осторожны при его запуске с тяжелыми `UPDATE`/`DELETE` или `INSERT` операциями. Всегда оборачивайте их в транзакции с последующим `ROLLBACK`!

```sql
EXPLAIN ANALYZE SELECT * FROM users WHERE email = 'user_5@example.com';
```

Пример вывода:
```text
Seq Scan on users  (cost=0.00..2.50 rows=1 width=34) (actual time=0.015..0.038 rows=1 loops=1)
  Filter: (email = 'user_5@example.com'::text)
  Rows Removed by Filter: 99
Planning Time: 0.082 ms
Execution Time: 0.055 ms
```
Здесь мы видим:
* `Filter` — условие фильтрации.
* `Rows Removed by Filter: 99` — сколько строк СУБД пришлось прочитать и отбросить, чтобы найти нужную строку. Прочитать 100 строк — быстро, но если в таблице будет 100 000 000 строк, такой запрос «положит» диск и процессор.

---

## Часть 3: Индексы (B-Tree)

Чтобы не сканировать таблицу целиком, создают **индексы**. Индекс — это вспомогательная структура данных (по умолчанию сбалансированное дерево — B-Tree), которая хранит отсортированные значения колонки и ссылки на строки в основной таблице.

1. **Автоматические индексы:**
   Первичные ключи (`PRIMARY KEY`) и ограничения уникальности (`UNIQUE`) автоматически создают индексы в PostgreSQL. Нам не нужно вручную создавать индекс для колонок `id` или `users.email` (так как на них висит unique constraint).

2. **Ручное создание индексов:**
   Для колонок, по которым часто идет фильтрация (`WHERE`) или соединение (`JOIN`), создают индексы вручную:
   ```sql
   CREATE INDEX idx_users_registration_date ON users(registration_date);
   ```

3. **Составные индексы (Composite Indexes):**
   Если в `WHERE` часто фильтруются две колонки одновременно, эффективнее создать один составной индекс:
   ```sql
   CREATE INDEX idx_products_cat_price ON products(category, price);
   ```

С активным индексом план выполнения запроса меняется с `Seq Scan` на **`Index Scan`** (или `Index Only Scan` — если все запрашиваемые данные есть в самом индексе, и лезть в таблицу не нужно).

---

## Часть 4: Troubleshooting

### Сценарий 1: Индекс создан, но СУБД всё равно делает Seq Scan
Вы создали индекс на колонку `registration_date` в таблице `users`. Но при запуске `EXPLAIN ANALYZE` видите `Seq Scan`.

**Причина и решение:**
1. **Маленький размер таблицы.** Если в таблице всего 100 строк (как на нашем тестовом стенде), планировщик решает, что прочитать один блок данных целиком быстрее, чем сначала читать индексный файл, а потом переходить к таблице. Чтение индекса добавляет накладные расходы. В таких случаях `Seq Scan` — это абсолютно нормальный выбор планировщика.
2. **Как проверить?** Для принудительного тестирования индекса на маленьких таблицах в рамках сессии можно отключить последовательные сканирования:
   ```sql
   SET enable_seqscan = off;
   EXPLAIN ANALYZE SELECT * FROM users WHERE registration_date = '2026-05-15';
   ```

### Сценарий 2: Использование функций «убивает» индекс
Разработчик написал запрос:
```sql
SELECT * FROM users WHERE EXTRACT(YEAR FROM registration_date) = 2026;
```
Этот запрос делает `Seq Scan`, хотя индекс на `registration_date` существует.

**Причина и решение:**
Индекс построен по значениям колонки `registration_date`, а не по результатам функции `EXTRACT`. Чтобы индекс заработал, нужно либо переписать запрос на поиск по диапазону дат:
```sql
SELECT * FROM users WHERE registration_date BETWEEN '2026-01-01' AND '2026-12-31';
```
Либо создать специальный функциональный индекс:
```sql
CREATE INDEX idx_users_reg_year ON users ((EXTRACT(YEAR FROM registration_date)));
```

---

## Вопросы для самопроверки
1. В чем разница между `EXPLAIN` и `EXPLAIN ANALYZE`?
2. Почему вредно создавать индексы абсолютно на все колонки таблицы?
3. Что такое `Index Only Scan` и почему он быстрее, чем обычный `Index Scan`?
4. Как неявное приведение типов (например, поиск строки в числовой колонке или наоборот) влияет на использование индексов?
