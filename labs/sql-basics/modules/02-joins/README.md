# Лабораторная работа 02: Соединение таблиц (JOIN)

## Оглавление
- [Часть 1: Нормализация и внешние ключи](#часть-1-нормализация-и-внешние-ключи)
- [Часть 2: Внутреннее соединение (INNER JOIN)](#часть-2-внутреннее-соединение-inner-join)
- [Часть 3: Левое внешнее соединение (LEFT JOIN)](#часть-3-левое-внешнее-соединение-left-join)
- [Часть 4: Детализация множества таблиц](#часть-4-детализация-множества-таблиц)
- [Часть 5: Troubleshooting - Декартово произведение](#часть-5-troubleshooting)
- [Вопросы для самопроверки](#вопросы-для-самопроверки)

Время: ~35 мин | Сложность: 3/5

Цель: научиться писать сложные запросы, связывающие данные из нескольких таблиц, понять механику работы внешних ключей и виды соединений.

---

## Часть 1: Нормализация и внешние ключи

В реляционных базах данных информация не дублируется. Данные о клиенте хранятся в одной таблице, а данные о его заказах — в другой. Связь осуществляется через Внешний Ключ (Foreign Key). Для объединения этих данных в единый результат используются операторы `JOIN`.

---

## Часть 2: Внутреннее соединение (INNER JOIN)

`INNER JOIN` (или просто `JOIN`) оставляет только те строки, для которых нашлось совпадение в обеих связываемых таблицах по условию `ON`.

```sql
SELECT orders.id AS order_id, users.name, orders.order_date
FROM orders
INNER JOIN users ON orders.user_id = users.id;
```
Если в таблице `users` есть клиент, который не совершил ни одного заказа, он НЕ попадет в итоговую выборку.

---

## Часть 3: Левое внешнее соединение (LEFT JOIN)

`LEFT JOIN` гарантирует, что ВСЕ строки из "левой" (первой указанной) таблицы попадут в результат. Если для строки из левой таблицы не нашлось совпадения в правой, соответствующие колонки правой таблицы будут заполнены значением `NULL`.

```sql
SELECT users.name, orders.id AS order_id
FROM users
LEFT JOIN orders ON users.id = orders.user_id;
```
Этот запрос выведет абсолютно всех клиентов. Те, кто ничего не покупал, будут иметь `NULL` в колонке `order_id`.

---

## Часть 4: Детализация множества таблиц

Построим сложный чек для Заказа №1, связав 4 таблицы:

```sql
SELECT 
    users.name AS customer_name,
    products.name AS product_name,
    order_items.quantity,
    order_items.price_per_unit
FROM orders
JOIN users ON orders.user_id = users.id
JOIN order_items ON orders.id = order_items.order_id
JOIN products ON order_items.product_id = products.id
WHERE orders.id = 1;
```

---

## Часть 5: Troubleshooting

**Сценарий:** Вы написали запрос для соединения таблицы `products` и `categories`:
```sql
SELECT * FROM products JOIN categories;
```
Запрос выполняется бесконечно долго, а результат содержит миллионы строк, хотя в таблицах их всего несколько тысяч.

**Причина и решение:** Вы забыли указать условие связывания `ON`. База данных выполнила `CROSS JOIN` (Декартово произведение), умножив каждую строку первой таблицы на каждую строку второй таблицы. Если в таблицах по 1000 строк, результат будет содержать 1 000 000 строк. Всегда явно указывайте условие `ON`!

---

## Вопросы для самопроверки
1. Чем отличается `RIGHT JOIN` от `LEFT JOIN`? Можно ли любой `RIGHT JOIN` переписать с использованием `LEFT JOIN`?
2. В каком случае `FULL OUTER JOIN` возвращает `NULL`? Приведите пример бизнес-задачи для такого типа соединения.
3. Разрешено ли соединять таблицу саму с собой (Self Join)? Как в таком случае решается конфликт имен таблиц?
