apiVersion: v1
kind: Pod
metadata:
  name: bad-pod
  namespace: lab-restricted
spec:
  containers:
  - name: app
    image: nginx:1.27-alpine
    # Нет securityContext! Работает от root, не сбрасывает caps.
