apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: broken-gw
  namespace: lab-gateway
spec:
  gatewayClassName: unknown-class
  listeners:
  - name: http
    protocol: HTTP
    port: 80
